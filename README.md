# aura-backend

MVP ioBroker adapter for the Aura app.

Version 1 provides WebSocket communication, token authentication, ioBroker object/state discovery, snapshots, live state updates, state writes, and notification-state forwarding.

## Development

```bash
npm install
npm run check
```
